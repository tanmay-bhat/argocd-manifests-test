apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-3
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/dummy/repo.git' # Replace with actual repo URL
    path: charts/chart-3
    targetRevision: HEAD
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
